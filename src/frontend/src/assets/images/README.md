# Image assets

- `photography/` contains optimized, delivery-sized photographs grouped further by feature when real assets are added.
- `placeholders/` contains neutral fallbacks that are safe to bundle with the application.
- Do not copy UI, text, or controls from design screenshots into this directory.
- Preserve meaningful Persian alt text at the component or API-contract boundary rather than encoding it in filenames.
