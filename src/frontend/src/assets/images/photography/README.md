# Photography delivery assets

Add only optimized frontend variants here. Group files by the owning feature when assets arrive, for example:

```text
photography/
├── home/
├── portfolio/
├── products/
└── album-previews/
```

Private gallery images are runtime object-storage resources and must never be committed to the application bundle.
